/**
 * Admin — Reviewer checklist management page entrypoint.
 * Keeps route naming aligned with Sprint 14 terminology.
 */

import ChecklistTemplatesPage from './ChecklistTemplatesPage'

/**
 * @returns {JSX.Element}
 */
export default function ReviewerChecklistAdminPage() {
  return <ChecklistTemplatesPage />
}
