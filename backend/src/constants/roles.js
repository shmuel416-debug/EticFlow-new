/**
 * EthicFlow — Role Constants
 * Shared role lists used for authorization checks and role-based filtering.
 */

export const COMMITTEE_ROLES = ['SECRETARY', 'REVIEWER', 'CHAIRMAN', 'ADMIN']
export const ROLE_RESEARCHER = 'RESEARCHER'
